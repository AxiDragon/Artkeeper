<Window x:Class="Artkeeper.MainWindow"
        xmlns:UserControls="clr-namespace:Artkeeper.UserControls"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        BorderThickness="0"
        ResizeMode="CanMinimize"
        Title="Artkeeper" Height="600" Width="600"
        FontSize="15"
        Background="{StaticResource DarkShade}"
        Foreground="{StaticResource DarkShade}"
        FontFamily="{StaticResource Roboto}">
    <Window.Resources>
        
    </Window.Resources>
    <Grid>
        <StackPanel Background="{StaticResource DarkShade}">
                <UserControls:TimerControl/>
        </StackPanel>
    </Grid>
</Window>
